# Copy this file to keystore.properties only when you have a private release keystore.
# Never commit the copied file or the keystore itself.
storeFile=../signing/xiamu-release.jks
storePassword=填入签名库密码
keyAlias=填入别名
keyPassword=填入密钥密码
